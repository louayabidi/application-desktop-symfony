{% extends 'evenement/evenementback.html.twig' %}

{% block title %}Participation{% endblock %}

{% block louay %}
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdP</th>
                <td>{{ participation.idP }}</td>
            </tr>
            <tr>
                <th>NomP</th>
                <td>{{ participation.nomP }}</td>
            </tr>
            <tr>
                <th>PrenomP</th>
                <td>{{ participation.prenomP }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ participation.email }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ participation.age }}</td>
            </tr>
               <th>evenement</th>
            <td>{{ participation.idfEvent.nomEve }}</td>


             <tr>
    <th>Image de l'évenement</th>
    <td>
        <img src="{{ asset('uploads/images/evenements/' ~ participation.idfEvent.imageEve) }}" alt="Image de l'événement" width="1000" height="1000">
    </td>
</tr>

        </tbody>
    </table>

    <a href="{{ path('app_participation_index') }}">back to list</a>

    <a href="{{ path('app_participation_edit', {'idP': participation.idP}) }}">edit</a>

    {{ include('participation/_delete_form.html.twig') }}




{% endblock %}
