<form method="post" action="{{ path('app_avisequipement_delete', {'idaeq': avisequipement.idaeq}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avisequipement.idaeq) }}">
    <button class="btn">Delete</button>
</form>
