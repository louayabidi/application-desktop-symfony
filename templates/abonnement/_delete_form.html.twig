<form method="post" action="{{ path('app_abonnement_delete', {'idab': abonnement.idab}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonnement.idab) }}">
    <button class="btn">Delete</button>
</form>
