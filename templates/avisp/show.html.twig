{% extends 'base.html.twig' %}

{% block title %}Avisp{% endblock %}

{% block body %}
    <h1>Avisp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idap</th>
                <td>{{ avisp.idap }}</td>
            </tr>
            <tr>
                <th>Commap</th>
                <td>{{ avisp.commap }}</td>
            </tr>
            <tr>
                <th>Star</th>
                <td>{{ avisp.star }}</td>
            </tr>
            <tr>
                <th>Fav</th>
                <td>{{ avisp.fav ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avisp_index') }}">back to list</a>

    <a href="{{ path('app_avisp_edit', {'idap': avisp.idap}) }}">edit</a>

    {{ include('avisp/_delete_form.html.twig') }}
{% endblock %}
